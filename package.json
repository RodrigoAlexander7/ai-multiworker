{
  "name": "ai-multiworker",
  "version": "0.1.0",
  "description": "Delegate token-heavy, low-reasoning work from Claude Code to cheaper Antigravity (agy) workers.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "bin": {
    "multiworker": "./src/interfaces/cli/main.js"
  },
  "scripts": {
    "test": "node --test test/",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm test"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
